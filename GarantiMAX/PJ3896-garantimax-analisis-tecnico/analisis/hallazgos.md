# Registro único de hallazgos

| Campo | Detalle |
|---|---|
| Requerimiento(s) | RNF-09 (priorización por severidad) |
| Versión | 1.0 (cierre de Fase 2 — T-21) |
| Fecha | 2026-08-24 |
| Estado | ✅ Consolidado — 8 hallazgos: 1 Crítico, 1 Alto, 3 Medio, 2 Bajo-Medio, 1 Bajo |

> Escala de severidad y protocolo de escalamiento en `00-metodologia-y-evidencia.md` §3. Ordenado por severidad descendente; dentro de cada severidad, por capítulo.

| # | Severidad | Dimensión | Capítulo | Descripción | Evidencia | Impacto | Recomendación | Etapa que lo confirma |
|---|---|---|---|---|---|---|---|---|
| 1 | **Crítico** | Seguridad | C11 (adelantado desde T-08/T-09) | La tabla `mora_corte` tiene RLS **activo** pero su política de lectura (`mora_corte_select`) es `for select using (true)` **sin ninguna condición** — ni `to authenticated`, ni `auth.uid() is not null`, ni `app_rol()`. No hay ningún `revoke` sobre esta tabla en el historial de migraciones (a diferencia de otras tablas sensibles del mismo repositorio, que sí lo tienen — ver evidencia). Con el comportamiento por defecto de Supabase (el rol `anon` recibe `GRANT` sobre tablas nuevas de `public` salvo revocación explícita, y no se encontró ningún `ALTER DEFAULT PRIVILEGES` que cambie eso), esto significa que **la `anon key` — pública, embebida en el bundle del frontend — puede leer la tabla completa sin autenticación**. | `supabase/migrations/0221_mora_aging_historico.sql:40-42` (única definición de la política, nunca redefinida — verificado con `grep -rn "mora_corte_select" supabase/migrations/*.sql`, un solo resultado). Contraste: `supabase/migrations/0146_solicitudes_fase1.sql:416-418`, `0152_portal_proveedor_login.sql:39-40`, `0184_hardening_solicitudes_tareas.sql:556` — mismo repositorio revocando `anon` explícitamente en otras tablas. **Migración 0329 (`tablas_sensibles_por_capacidad.sql`) endureció `mora_clientes` y `mora_vinculos` por el mismo motivo — su comentario dice que "espeja el gate que ya tenía `mora_corte`" — pero `mora_corte` en sí quedó sin tocar.** | `mora_corte` contiene, por cliente: monto vencido, monto total, tramo de mora, **`bloqueado`**, **`dicom`** (lista negra crediticia en Chile) y **`cobranza_externa`**. Es información financiera y crediticia de clientes reales, expuesta sin autenticación si el análisis es correcto. | **Verificar contra la base real en T-32 (Etapa B) con máxima prioridad** — es la primera tabla a revisar en cuanto llegue A1. Si se confirma: aplicar de inmediato el mismo patrón de `0329` (gate por `app_rol() in ('CM','GTE')` o capacidad `cobranza`) y/o `revoke all on public.mora_corte from anon`. | **A (código) — confirmado por lectura directa del SQL; B (T-32) verifica que la base productiva coincide con la migración** |
| 2 | **Alto** | Testing / Proceso | C13 (T-19) | **Cero tests de componentes o de interacción de UI en todo el sistema** — no hay `@testing-library/react`, `playwright` ni `cypress` en las dependencias. Los 65 archivos de test cubren exclusivamente lógica pura (parsers, cálculos de dominio, guarda de migraciones); ninguno renderiza un componente ni simula una acción de usuario. 12 de 22 módulos con código no tienen ningún test, incluidos `incentivos` (cálculo de compensación), `mora` (donde vive el hallazgo #1) y `portal` (superficie pública). | `package.json` (ausencia de las tres dependencias); inventario completo de los 65 archivos de test por módulo (`inventarios/archivos-test.txt`); `CasoDetalle.tsx` (3525 líneas, C10) sin ningún test que lo ejercite. | Cualquier refactor de UI —empezando por los componentes más grandes del sistema— no tiene red de seguridad automatizada; los errores de interacción solo se detectan manualmente o en producción. | Priorizar tests de componente/interacción para los flujos críticos de `postventa` (el módulo más grande y con más lógica en RPC, C2) antes de cualquier refactor mayor; extender cobertura de lógica pura a los 12 módulos sin tests, empezando por `incentivos` y `mora`. | **A (código) — verificado por ausencia de dependencias y por inventario completo** |
| 3 | **Medio (histórico, ya remediado)** | Seguridad / Proceso | C4/C11/C13 (adelantado desde T-11) | La Edge Function `dossier-ia` (genera el briefing del 1:1 con IA, con costo de tokens de Anthropic) estuvo desplegada **sin exigir autenticación** (`verify_jwt=false`) hasta el 03-08-2026. El propio comentario del código la identifica como "brecha #6", lo que implica que hubo al menos 5 brechas numeradas anteriores, corregidas pero sin registro centralizado. | `supabase/functions/dossier-ia/index.ts` (comentario de cabecera, líneas 1-9): *"verify_jwt=TRUE desde el 03-08-2026 (brecha #6): estaba desplegada sin exigir [autenticación]"*. | Ya remediado — no es explotable hoy. El impacto de la brecha original habría sido costo/abuso de la API de Anthropic por invocación no autenticada, no exposición de datos de negocio. | No requiere acción correctiva (ya corregida). **Recomendación de proceso:** centralizar el registro de brechas encontradas y corregidas (hoy disperso en comentarios de código) en un documento de bitácora de seguridad — insumo para C13/C14. | **A (código) — el propio comentario documenta la fecha y el motivo de la corrección** |
| 4 | **Medio** | Arquitectura / Tipado | C10 (adelantado desde T-16) | Ni `tsconfig.json` ni `tsconfig.app.json` declaran `"strict": true`. `strictNullChecks` y `noImplicitAny` están **apagados por defecto** — dos de las protecciones de mayor impacto de TypeScript no operan en un sistema de 113 587 líneas con lógica financiera. | `tsconfig.json`, `tsconfig.app.json` (ausencia de la clave `strict`); `eslint.config.js` usa `tseslint.configs.recommended`, no `recommendedTypeChecked` ni `strict`. | Errores del tipo "no se esperaba `null`/`undefined` aquí" no se detectan en compilación, solo en ejecución — deuda técnica con probabilidad real de materializarse (ya lo hizo una vez con el canal Realtime duplicado, C10 §2, aunque ese caso no es de tipado). | Activar `strict: true` de forma incremental (archivo por carpeta o con `// @ts-nocheck` temporal en lo no migrado) antes de cualquier escenario que dependa de la robustez del tipado actual como argumento para "conservar el front". | **A (código) — configuración verificable directamente** |
| 5 | **Medio** | Arquitectura / Tipado | C10 (adelantado desde T-16) | No se encontró evidencia de tipos generados automáticamente desde el esquema de Supabase (`supabase gen types`); el acceso a datos usa tipado manual (`as Record<string, unknown>`, interfaces escritas a mano). Combinado con el hallazgo #3, un cambio de columna en una migración no rompe la compilación del front. | Búsqueda de archivo de tipos generado en `src/` durante T-16: sin resultado. Patrón repetido de `as` en accesos a Supabase observado en múltiples módulos (C2/T-07). | Silencioso hasta que ocurre en producción — mismo perfil de riesgo que #3, con el que está directamente relacionado. | Adoptar `supabase gen types typescript` como paso de CI o de build, generando el archivo de tipos desde el esquema real — resuelve este hallazgo y reduce parcialmente el de #3 para la capa de datos específicamente. | **A (código) — ausencia verificada por búsqueda directa; B podría confirmar contra el esquema real si se genera el archivo entonces** |
| 6 | **Bajo-Medio** | Seguridad | C11 (T-17) | 34 de las 46 Edge Functions responden `Access-Control-Allow-Origin: '*'` (cualquier origen); solo 8 validan el origen explícitamente contra una lista permitida (`ORIGEN_OK`). Riesgo bajo en la práctica porque estas funciones exigen JWT (CORS no elude la autenticación por token), pero es una política inconsistente. | `grep` sobre las 46 funciones: 34 con `'*'` literal, 8 con el patrón `ORIGEN_OK.test(...)`. | Bajo si todas las funciones con `'*'` exigen JWT (a confirmar una por una si se profundiza); las 8 restringidas son, no por casualidad, las administrativas, la que tuvo la brecha #6, y todo el subsistema de portal externo — la priorización ya está bien dirigida. | Estandarizar la validación de origen en las 34 restantes usando el mismo patrón `ORIGEN_OK` ya probado — bajo costo, cierra el margen por completo. | **A (código) — clasificación completa de las 46 funciones** |
| 7 | **Bajo-Medio** | Observabilidad | C14 (T-20) | Sentry (única herramienta de observabilidad del sistema) **no tiene ninguna presencia en las 46 Edge Functions** — es exclusivamente un mecanismo de frontend. Los 10 cron jobs de negocio no tienen ninguna alerta de infraestructura: si uno falla sistemáticamente (ej. `monedas-cron` sin actualizar ninguna moneda), nadie se entera salvo revisión manual de los logs de Supabase. | `grep -rl "sentry" supabase/functions` → 0 resultados. Patrón representativo en `tareas-atrasadas-cron/index.ts:592`: el error se devuelve en la respuesta HTTP del cron, que nadie inspecciona en el momento. | Fallos de backend (notificaciones, cálculos financieros, cron) pueden pasar desapercibidos por tiempo indefinido — es la causa raíz estructural detrás de por qué las "brechas numeradas" (#3) se descubren tarde y de forma manual. | Extender observabilidad de errores a las Edge Functions (Sentry tiene SDK para Deno) o, como mínimo, una alerta simple (correo/Slack) en el `catch` de nivel superior de los 10 cron jobs. | **A (código) — ausencia confirmada por búsqueda directa en las 46 funciones** |
| 8 | **Bajo** | Rendimiento | C12 (T-18) | `useVentasSimuladas.ts` (War Room) consulta `contratos` filtrando por 1-3 días recientes, sin `.limit()` explícito — depende implícitamente de que ningún día tenga más de 1000 contratos nuevos (límite de PostgREST). Razonable hoy, no garantizado por código. | `src/features/warroom/useVentasSimuladas.ts:70-77`. | Si el volumen de ventas diario creciera por encima de 1000, PostgREST truncaría en silencio, sin error visible — el War Room mostraría cifras incompletas sin avisar. | Agregar `.limit()` explícito con un techo generoso (ej. 5000) y loguear si se alcanza, para que el truncamiento silencioso se vuelva visible si ocurre. | **A (código) — patrón de consulta verificado directamente** |

---

## Nota sobre este hallazgo (T-04 §3 — protocolo de escalamiento)

Detectado el 24-08-2026 durante T-08/T-09 (reconstrucción del modelo de datos), **antes** de tener acceso de lectura a la base (A1 pendiente). La evidencia es 100% del código SQL versionado — no se ejecutó ninguna consulta contra la base productiva, y no se intentó verificar la explotabilidad real (el análisis no toca el sistema, RNF-01). Se notifica a Javier Oropeza en el mismo mensaje que reporta este avance, para que él escale a Aldo Álvarez el mismo día, según el protocolo. No se profundiza más de lo necesario para describir el hallazgo — coherente con el paso 1 del protocolo de `00-metodologia-y-evidencia.md`.

---

## Consolidación (T-21) — cierre de Fase 2

**8 hallazgos**, 100% con evidencia citada (archivo y línea, o comentario de código textual — RF-23), ninguno sin severidad asignada (RNF-09). Distribución:

| Severidad | Cantidad | # |
|---|---|---|
| Crítico | 1 | #1 |
| Alto | 1 | #2 |
| Medio | 3 (1 histórico, ya remediado) | #3, #4, #5 |
| Bajo-Medio | 2 | #6, #7 |
| Bajo | 1 | #8 |

**Lectura agregada para el dictamen (C18/T-26):** de los 8, solo 1 (#1, `mora_corte`) es una exposición de datos activa y no remediada — y su confirmación depende enteramente de Etapa B (T-32). El resto es deuda técnica de proceso y calidad (testing, tipado, CORS, observabilidad) — real, pero de la clase que se corrige con trabajo planificado, no con una intervención de emergencia. **Ningún hallazgo, por sí solo, determina la elección entre refactorizar y rehacer** — son insumo para C16/T-24, no una conclusión anticipada.

**Patrón transversal que conecta varios hallazgos:** #2 (sin tests de UI), #4/#5 (tipado débil) y #7 (sin observabilidad en Edge Functions) comparten una misma causa raíz — **ausencia de redes de seguridad automatizadas más allá de la lógica de dominio pura**. El sistema invirtió bien en testear y tipar lo que es fácil de aislar (cálculos, parsers) pero no en lo que rodea esa lógica (UI, integración, infraestructura). Es una lectura útil para C10/C19: no es negligencia difusa, es una inversión consistente pero incompleta.
