# Registro de Avance — CMS Autoexplora (Strapi)

> Este documento lo actualiza Claude Code automáticamente conforme ejecuta tareas del plan. Si otro compañero retoma el trabajo, debe leer este archivo primero para entender el estado actual.

| Campo | Detalle |
|---|---|
| Plan de origen | `PLAN.md` |
| Repositorio | `autoexplora-cms` — `git@github.com:Sitios-Web-Go-Virtual/autoexplora-cms.git` (CMS) y `autoexplora-alfa` — `git@github.com:Sitios-Web-Go-Virtual/autoexplora-alfa.git` (sitio, desde T-11) |
| Rama | `feature/PJ5040-cms-autoexplora-mvp` en ambos repos (en `autoexplora-alfa`, creada desde `dev`, actualizada con un merge de `dev` el 2026-07-28 para T-15) |
| Responsable actual | Sharon Mendoza |
| Última actualización | 2026-08-04 |
| Modelo de ejecución | claude-sonnet-5 — esfuerzo: máximo |
| Estado general | 🟡 En progreso |

---

## Resumen de estado

**Fase 0 completada. Fase 1 (P1) completa: T-06 a T-12.** CMS (`autoexplora-cms`) con Postgres local, S3 con lectura/escritura/CORS/CSP verificados, rol Editor acotado a Banner, content type Banner (Dynamic Zone imagen/video, desktop+mobile), validación de archivos, reordenamiento automático por vigencia, publicación en dos etapas con webhook, y registro de auditoría (create/update/publish/unpublish, verificado en vivo con usuario real). El sitio (`autoexplora-alfa`) ya consume banners reales del CMS desde T-11. Despliegue del CMS: EC2 + Nginx + systemd (sin Docker), instancia aún no aprovisionada (solicitada a Alexis, 2026-07-20). **Fase 2 (P2 — blog): T-13 y T-14 completas.** `content` de Article terminó como **Dynamic Zone** de componentes (párrafo/encabezado/cita/imagen/lista/embed), no como editor de Bloques nativo — pivote forzado por un límite duro de Strapi descubierto al implementar T-14 (ver Decisiones). Editor amigable para redactar posts sin tocar código, con bloque de Embed de YouTube/redes (detección de proveedor por URL, sin script de terceros). **Fase 2 completa: T-13, T-14 y T-15.** El sitio (`autoexplora-alfa`) ya consume el blog real de Strapi (listado `/blog` y detalle `/blog/[slug]`, con SSG vía `generateStaticParams`), incluyendo el bloque de Embed. **Guardarraíl del PRD:** con P1 completo, el compromiso mínimo del 31 jul ya está cubierto; P2 está también completa. **Mejora de UI del admin (fuera de plan, RNF-08) avanzando "poco a poco" a petición del programador:** tema oscuro completo (logo, color de marca, gradiente de fondo), títulos de pestaña con marca, idioma español por default, copy e iconografía corregidos — sigue sin ser exhaustiva, se sigue retomando incrementalmente. **T-19 (Fase 4) adelantado y completo para QA (2026-07-31):** el CMS ya está desplegado y accesible públicamente en `https://qa-autoexplora-cms.govirtual.com.mx:8080` (EC2 + Nginx + systemd + Postgres local + certificado SSL real vía Certbot), con backup diario de Postgres automatizado, y el preview de Vercel de `autoexplora-alfa` ya consume ese CMS QA real (banners y blog visibles end-to-end). Pendiente de T-19: instancia de **producción** (separada de QA, ya confirmado con Alexis) y **alertas de facturación de AWS** (solicitadas a Alexis, sin respuesta aún). **Dos mejoras adicionales fuera de plan (2026-08-03), pedidas directo por el programador:** (1) el post del blog ya no deriva su fecha de `publishedAt`/`createdAt` (metadatos de Strapi) — Article tiene un campo editorial `publicationDate` (Custom Field que se autorrellena con "hoy" al crear pero es editable a cualquier fecha, incluso pasada, para republicar contenido viejo), que ahora define tanto la fecha mostrada como el orden del listado `/blog`; (2) el campo `order` de Banner deja de editarse a mano (causaba huecos y confusión sobre qué banner era cuál) — se asigna solo al final al crear, y una página nueva del admin ("Homepage banners") permite reordenar por drag & drop, con un endpoint propio que renumera 1..N. Ambas verificadas end-to-end y desplegadas a QA el mismo día, junto con un **bug real de producción encontrado y corregido**: el CSS de marca del admin (gradiente, logo dark, subtítulo del login oculto) nunca cargaba en un build de producción (`strapi build`/`strapi start`) aunque sí funcionaba en `strapi develop` — Vite nunca disparaba la carga del chunk de CSS separado; se resolvió inyectando esos estilos por JavaScript en `bootstrap()` en vez de por `import` de `.css`. Siguiente paso: Fase 3 (P3 — textos estáticos, T-16/T-17) si el tiempo lo permite, cerrar T-19 (producción + billing), o ambos en paralelo.

---

## Tareas completadas ✅

| ID | Tarea | Completada por | Fecha | Notas |
|---|---|---|---|---|
| T-01 | Scaffold de Strapi con TypeScript | Claude Code | 2026-07-16 | Verificado: `npm run develop` levanta el admin contra Postgres local |
| T-02 | Configurar Strapi con PostgreSQL (dev local + Postgres local en EC2 para qa/prod) | Claude Code | 2026-07-16 | Postgres local vía Postgres.app (sin Homebrew); qa/prod 100% por env vars/Secrets Manager, sin hardcode |
| T-03 | Conectar Media Library a S3 | Claude Code | 2026-07-17 | ✅ Completamente verificado, incluyendo lectura pública. Subida real confirmada en `govirtual-autoexplora-cms-qa`. Fix de subida: `config/plugins.ts` necesita `ACL: undefined` explícito (bucket con ACLs deshabilitadas, "Bucket owner enforced"). Lectura pública requirió **dos pasos** en AWS (aplicados por Alexis Herrera, 2026-07-17): (1) bucket policy de `s3:GetObject` público, (2) destildar las 2 opciones de "Block Public Access" relacionadas a bucket policies. Verificado con `curl` → `200 OK` sobre la URL directa del objeto. |
| T-04 | Ejecutar `/init` en el repo del CMS | Claude Code | 2026-07-16 | `CLAUDE.md` generado con stack, comandos y arquitectura |
| T-05 | Nginx + systemd para despliegue en EC2 *(rehecha 2026-07-17)* | Claude Code | 2026-07-16 → 2026-07-17 | Versión original (Dockerfile + ECS/Fargate, verificada con Docker Desktop) **retirada** por decisión de infraestructura del programador. Versión actual: `deploy/nginx.conf`, `deploy/strapi.service`, `deploy/README.md` — no verificable hasta que exista la instancia EC2 real (la crea el equipo de infraestructura) |
| T-06 | Autenticación y rol único "editor" | Claude Code | 2026-07-17 | Corrección de alcance sobre el plan: el mecanismo correcto es el RBAC nativo del Admin Panel (rol "Editor" de fábrica), no el plugin Users & Permissions (ese es para usuarios finales de un sitio público). Permisos otorgados por bootstrap idempotente (`src/bootstrap/ensureEditorPermissions.ts`). Verificado en BD: solo Banner + Media Library, sin Settings/Users/Roles |
| T-07 | Content type Banner | Claude Code | 2026-07-17 | Rediseñado durante pruebas manuales (ver Decisiones): Dynamic Zone con componentes Imagen/Video, desktop+mobile en ambos, sin campo `sección` ni `altText` propio. CRUD y Draft & Publish verificados por el programador en el admin |
| T-08 | Validación de formato y peso de archivos | Claude Code | 2026-07-20 | Alcance reducido: póster obligatorio ya resuelto en T-07 (schema), esta tarea solo valida formato/peso. Implementado como hook global (`strapi.db.lifecycles.subscribe` sobre `plugin::upload.file`) — aplica a todo el CMS, no solo Banner. Verificado: 8 casos unitarios en los límites exactos + prueba manual en el admin |
| T-09 | Reordenamiento automático de banners por vigencia | Claude Code | 2026-07-20 | Dos disparadores: cron cada 5 min (respaldo pasivo) + hook reactivo en afterCreate/afterUpdate (inmediato, con guardia anti-reentrancia). Banner expirado se despublica vía Document Service (`unpublish`), preservando el borrador (reversible). Verificado con script standalone: el hook reactivo recompactó automáticamente al crear los datos de prueba, antes de la llamada manual |
| T-10 | Publicación en dos etapas (API + webhook) | Claude Code | 2026-07-20 | **Hallazgo:** el plan decía `publicationState` (sintaxis Strapi v4) — corregido a `status=draft\|published` (Strapi v5), verificado empíricamente con token real. Webhook registrado por código (idempotente), verificado end-to-end con receptor HTTP local: `entry.publish`/`entry.unpublish` llegaron con el secreto correcto al publicar/despublicar un banner real |
| T-11 | Integración mínima en el sitio `autoexplora-alfa` | Claude Code | 2026-07-20 | Primer cambio de código en `autoexplora-alfa` (rama `feature/PJ5040-cms-autoexplora-mvp` creada desde `dev`). Fetch server-only (`lib/server/strapiApi.ts`) invocado directo desde `app/page.tsx` (Server Component) — sin ruta `/api/*` intermedia, decisión deliberada para no construir el endpoint de revalidación instantánea todavía (ISR de 2 min es suficiente por ahora). `HomeCarousel.tsx` pasó de array hardcodeado a prop `slides`; de paso se corrigió el LCP (estaba fijo al índice 2, debe ser el 0). Token de solo lectura para el sitio, verificado con `POST → 403`. Verificado en navegador: borrador visible, vacío sin error, publicado visible |
| T-12 | Registro de auditoría | Claude Code | 2026-07-20 | Content type `AuditLog` sin Draft & Publish. Implementado con `strapi.documents.use()` (no un hook de BD genérico) para distinguir sin ambigüedad create/update/publish/unpublish — internamente publicar es un delete+create que un hook de BD no puede diferenciar de forma confiable de una edición. Usuario obtenido de `strapi.requestContext.get().state.user`. Escritura del log en try/catch, nunca bloquea la acción real. **Verificado en vivo: las 4 acciones sobre un banner real quedaron registradas con el usuario correcto. Completa el alcance P1 del MVP (T-06 a T-12).** |
| *(fuera de plan)* | Mejora **parcial** de UI del admin (RNF-08) | Claude Code | 2026-07-23 → 24 | No es una tarea T-XX del plan — pedido directo del programador. Logo de marca (login + menú), color `#02132D` reemplazando el azul default de Strapi (se probó también `#FFB000`, se descartó), etiquetas/descripciones en español para Banner/componentes/AuditLog vía `src/bootstrap/ensureSpanishLabels.ts` (mismo patrón idempotente que roles/webhooks). **Deliberadamente incompleta** — se retoma para pulir más adelante (más descripciones, revisión de otros textos), priorizando ahora Fase 2. Retomada 2026-07-29, ver fila siguiente. |
| *(fuera de plan)* | Mejora de UI del admin — segunda ronda: tema claro/oscuro completo | Claude Code | 2026-07-29 | Continúa la ronda anterior, pedido directo del programador ("ir haciendo mejoras poco a poco"). **Contenido:** descripciones de ayuda faltantes (Banner imagen/video con formato/peso máximo — reusa los límites de T-08 —, `content` de Article, `article.image`/`list`); descripción del rol Editor traducida; ícono distinto por tipo de bloque en los selectores de Dynamic Zone de Article (6) y Banner (2) — antes todos compartían el ícono genérico de `apps`; idioma español por default (Strapi no lo soporta por config — se pre-siembra la key de `localStorage` que usa su propio selector, desde `bootstrap()`); copy del login corregido (se quita el subtítulo redundante, "Bienvenido!" → "¡Bienvenido!", espaciado parejo); títulos de pestaña con marca ("Brick \| Inicio", "Brick \| Inicio de sesión", etc. — sin hook oficial de Strapi para esto, se intercepta `document.title` con `MutationObserver` + parcheo de History API, ya que las rutas de auth no usan el `Page.Title` interno del resto de páginas); y **tema oscuro completo** (gradiente de fondo, escala de color de marca, y logo — todos distintos de tema claro). **Hallazgo técnico:** Strapi no expone ningún atributo en el DOM que indique qué tema (claro/oscuro) está activo — se resuelve leyendo su propia key de `localStorage` (`STRAPI_THEME`, con fallback a `matchMedia('prefers-color-scheme: dark')` si está en modo "system") y escribiendo un atributo propio en `<html>` (`data-brick-theme`) para poder engancharse desde CSS externo. El logo del menú lateral cambió del logo horizontal al favicon cuadrado (era ilegible en un espacio tan chico); ambos logos (login y menú) cambian de imagen en dark vía `content: url(...)` en CSS, sin tocar `config.auth.logo`/`config.menu.logo` (Strapi solo acepta un valor único, no uno por tema). **Fragilidad documentada:** varias reglas de `background.css` dependen de clases generadas por styled-components (`.dDObpJ`, `.NFNTJ`, obtenidas inspeccionando el DOM real con el programador) o del nombre de archivo de nuestros propios assets — sin ningún atributo estable expuesto por Strapi; están comentadas inline advirtiendo que pueden dejar de aplicar en una futura actualización de `@strapi/admin`/`@strapi/design-system`. Verificado en vivo por el programador en cada paso (arranque limpio + revisión visual). Sigue sin ser exhaustiva (aún puede haber textos en inglés sueltos en pantallas no revisadas) — se sigue retomando "poco a poco" a petición del programador. |
| T-13 | Content type Article (inicia P2) | Claude Code | 2026-07-24 → 27 | Diseñado a partir de la estructura **real** del blog en `autoexplora-alfa` (revisada por instrucción explícita del programador), no solo del PRD abstracto. Desviaciones confirmadas: sin `Category`/`Tag` (fuera de alcance, posts no categorizados); `hero` es campo de **imagen manual e independiente** (no video, no derivado del cuerpo) — fondo del hero banner del post + miniatura en el listado `/blog` (el más reciente primero), para que quien redacte no configure nada aparte de subir la imagen. Extiende permisos de Editor, auditoría (`strapi.documents.use()`) y etiquetas en español (`ensureSpanishLabels.ts`) al nuevo content type. **Verificado en vivo:** creación de un artículo real por el programador — auditoría (`create`) y relación `hero`→archivo confirmadas en BD. **Hallazgo de UX (no es bug):** el auto-relleno del slug (campo `uid`, `targetField: title`) a veces no reacciona en vivo al título y muestra el nombre singular del content type (`article`) hasta dar clic en "Regenerar" — es comportamiento del propio editor de Strapi, no de nuestro schema; se ajustó el texto de ayuda del campo para advertirlo. **`content` rediseñado el 2026-07-27** de campo `blocks` a Dynamic Zone — ver fila T-14. |
| T-15 | Consumo de blog en el sitio `autoexplora-alfa` (completa P2) | Claude Code | 2026-07-28 | **Prerrequisito de rama:** `autoexplora-alfa` ya se había lanzado a producción (`post-lanzamiento`) y divergido de `dev` (base original de T-11). Se confirmó con el programador que `dev` ya estaba sincronizada con `post-lanzamiento` (0 commits de diferencia) y se mergeó `dev` hacia `feature/PJ5040-cms-autoexplora-mvp` antes de tocar código — build sobre el código real de producción, no sobre una base desactualizada de julio. Merge limpio, sin conflictos (17 archivos), pusheado con autorización. **Diseño:** se preservó 100% la forma existente (`Post`/`ContentBlock` en `app/blog/data/posts.ts`, `PostCard`, `BlogPostLayout.tsx`) — solo se reemplazó el mock (`POSTS`/`getPost`) por fetch real a Strapi (`lib/server/strapiApi.ts`: `fetchBlogPosts`, `fetchBlogPostBySlug`, `fetchBlogSlugs`), con un mapper que traduce cada componente de la Dynamic Zone (`__component`) al `ContentBlock` que el sitio ya sabía renderizar, más el nuevo caso `embed` (con la misma lógica de detección de proveedor por URL que se diseñó para el CMS, replicada en `lib/blog/embedProvider.ts` porque son dos codebases separadas). `app/sitemap.ts` y `generateStaticParams` también migrados a slugs reales. **Hallazgo 1:** `populate[hero]=*` (comodín) falla en Strapi 5.50.2 para un campo `media` simple con `ValidationError: Invalid key related at hero.related` — se corrigió a `populate[hero]=true` (los componentes dentro de la Dynamic Zone sí aceptan `=*` sin problema; solo el campo de nivel superior tiene este comportamiento). Verificado con curl directo contra la API antes de tocar el código de Next.js. **Hallazgo 2:** en `status=draft` (preview), Strapi devuelve siempre la versión borrador de cada entrada, cuyo `publishedAt` es `null` sin importar si ya existe una versión publicada de esa misma entrada — el `sort=publishedAt:desc` nativo de la API no ordena nada en ese modo (todo empata en null), reproducido en vivo: un artículo sin publicar aparecía antes que uno recién publicado. Se resolvió con `createdAt` como respaldo de fecha (`entry.publishedAt ?? entry.createdAt`) y reordenando en el cliente dentro de `fetchBlogPosts()`, en vez de depender del sort de la API — en producción (`status=published`) es inofensivo porque ahí `publishedAt` siempre es real. **Permisos del token:** el token de solo lectura del sitio (`autoexplora-alfa-site-read`) solo tenía permisos de Banner; se creó `src/bootstrap/ensureSiteApiTokenPermissions.ts` en el CMS (mismo patrón idempotente que `ensureEditorPermissions.ts`) para agregarle `api::article.article.find`/`.findOne` automáticamente en cualquier ambiente, sin tocar el admin a mano. **Verificado en vivo:** `npm run build` generó estáticamente `/blog/[slug]` para el slug real (SSG); el programador publicó un artículo real y un banner en borrador desde el admin del CMS — ambos aparecieron correctamente en el sitio local (listado ordenado, detalle con el embed de YouTube renderizado como iframe real) sin ningún cambio adicional de código. |
| T-19 (parcial: QA) | Despliegue del CMS a un ambiente real de QA (adelanta Fase 4) | Claude Code + Sharon Mendoza | 2026-07-30 → 31 | **Primer despliegue real del proyecto**, guiado paso a paso por SSH (Termius) contra una instancia EC2 nueva (Ubuntu 26.04) aprovisionada por Alexis Herrera. Se siguió `deploy/README.md` con **dos ajustes específicos de esta instancia**: ruta de instalación `/usr/local/qa-autoexplora-cms` (no `/var/www/...`) y puertos 80 (HTTP)/**8080** (HTTPS, no el 443 estándar) — ambos indicados por Alexis. Base de datos `qa_autoexplora_cms` (prefijo `qa_` por convención de nombres de Alexis, distinto del sufijo `_dev` usado en local), rol `strapi_cms` (sin prefijo `qa-`, por no ser un recurso de AWS rastreado). Acceso del servidor a GitHub vía Deploy Key (SSH, sin acceso de escritura). Proceso administrado con `systemd` (unit `strapi.service`, `WorkingDirectory` ajustado), Nginx como reverse proxy en ambos puertos, sin bloque HTTPS activo hasta tener DNS + certificado. Backup diario de Postgres (`pg_dump` + gzip + rotación de 14 días) vía cron a las 3:00 AM, **local a la instancia** — un bucket S3 privado dedicado para backup externo queda pendiente (el bucket de media library existente es de lectura pública, no apto para datos sensibles). DNS: Alexis configuró `qa-autoexplora-cms.govirtual.com.mx` en Cloudflare en modo "DNS only" (sin proxy) apuntando a la IP pública de la instancia (`100.49.87.251` — distinta de la IP privada de VPC `172.31.70.93` que aparecía en el prompt de la terminal, fuente de una confusión inicial). Certificado SSL real generado con `certbot certonly --nginx` (sin dejar que Certbot reescribiera la config de Nginx, ya que asume el puerto 443 estándar) — renovación automática programada por el propio Certbot. **Bloqueador de red resuelto:** el puerto 8080 no respondía desde internet aunque Nginx sí escuchaba bien localmente (`curl` a `localhost:8080` funcionaba, desde afuera daba timeout) — Security Group de AWS sin la regla de entrada (inbound) para 8080; Alexis la agregó y quedó resuelto. **Integración con Vercel:** se generó un token de solo lectura (`autoexplora-alfa-site-read`) en el Strapi QA nuevo y se configuraron `STRAPI_API_URL`/`STRAPI_API_TOKEN`/`STRAPI_PUBLICATION_STATUS` como variables de entorno de Vercel, **con scope limitado a "Preview"** (nunca "Production") para el proyecto `autoexplora-alfa`. **Incidente de seguridad menor:** el programador pegó el token completo en el chat al depurar — se le indicó tratarlo como comprometido y regenerarlo de inmediato (se hizo). **Bug real encontrado y corregido:** el bloque de contenido (banners/blog) no aparecía en el preview de Vercel a pesar de que la API de Strapi respondía bien — se diagnosticó agregando temporalmente `console.error` a `fetchHomeBanners()` (commit revertido después de resolver) y revisando los **Build Logs** de Vercel (no los Runtime Logs generales del proyecto, que solo muestran tráfico de producción) — reveló un `403 Forbidden`: el valor de `STRAPI_API_TOKEN` guardado en Vercel no coincidía con el token real (típico de un copy-paste con espacio/salto de línea de más, o el token viejo antes de regenerarlo) — se resolvió borrando y volviendo a crear la variable en Vercel. **Nota técnica aparte:** `curl` interpreta corchetes (`[`/`]`) en URLs como su sintaxis de rangos (`archivo[1-10].html`), rompiendo cualquier prueba manual de los endpoints de Strapi con populate anidado (`populate[content][on][...]`) a menos que se use el flag `-g`/`--globoff` — no afecta al código real (Node/`fetch()` no tiene este problema, es exclusivo de la CLI de `curl`), pero sí puede confundir el diagnóstico manual si no se conoce. **Verificado en vivo end-to-end:** el preview de Vercel de la rama `feature/PJ5040-cms-autoexplora-mvp` muestra banners y artículos reales publicados desde el Strapi de QA. **Pendiente de T-19:** instancia de producción (separada, confirmada con Alexis) y alertas de facturación de AWS (solicitadas, sin respuesta). |
| *(fuera de plan)* | Fecha de publicación editorial de Article (Custom Field) | Claude Code | 2026-08-03 | Pedido directo del programador: un selector de fecha en el primer bloque del formulario (junto a título/autor/slug) que preseleccione "hoy" al crear pero sea editable a cualquier fecha, incluso pasada (para republicar un post viejo) — retira la lógica anterior de derivar la fecha de `publishedAt`/`createdAt`. **Investigado antes de implementar** (a petición del programador, "dime el plan que ejecutarás"): Strapi no soporta un `default` dinámico en el schema (siempre es un literal estático) y la validación Yup del lado del admin bloquea el guardado de un campo `required` vacío antes de que cualquier lifecycle hook del servidor lo vea — así que ni un `default` ni un `beforeCreate` bastan solos. Se resolvió con un **Custom Field** (`global::todayDate`) que envuelve el `DatePicker` nativo de Strapi y autorrellena la fecha de hoy en el cliente al montar si el valor viene vacío. **Dos bugs no triviales depurados en vivo:** (1) `TypeError: e.toISOString is not a function` al crear — `InputRenderer` de Strapi esparce el `value`/`onChange` crudos de `useField` sobre el componente junto con el resto de props; un `{...props}` genérico los sobrescribía después del `value` calculado explícito. (2) el campo mostraba el placeholder nativo `dd/mm/aaaa` en vez del picker de Strapi y no reflejaba visualmente la selección — causa real (confirmada leyendo el código fuente de `@strapi/design-system`): el mismo `{...props}` filtraba también `type: 'date'` (el tipo del atributo del schema) hasta el `<input>` nativo, convirtiéndolo en un input HTML `type="date"` de verdad. Se resolvió quitando por completo el spread genérico, dejando solo los props explícitos que el componente realmente necesita. **Bug de producción real encontrado por el programador (no por QA):** las fechas se mostraban un día antes de la seleccionada (ej. "1 de mayo" se guardaba pero el sitio mostraba "30 de abril") — causa: una fecha sin hora (`"YYYY-MM-DD"`) se interpreta como medianoche UTC; sin forzar `timeZone: "UTC"` en `Intl.DateTimeFormat`, se formatea en la zona del servidor (México, GMT-6), restando un día. Corregido en `formatSpanishDate()`. **Efecto secundario esperado:** los 2 artículos de prueba creados antes de este campo quedaron con `publicationDate` nulo, y Postgres ordena `NULL` primero en `ORDER BY ... DESC` — aparecían como "más recientes" hasta que el programador los volvió a guardar (autorrellena "hoy" al abrir un valor vacío). **Verificado en vivo:** creación con default de hoy, edición a fecha pasada, y el listado `/blog` reordenándose correctamente por la nueva fecha. |
| *(fuera de plan)* | Reorden de banners por drag & drop y posición automática | Claude Code | 2026-08-03 | Pedido directo del programador tras notar que `order` (entero de texto libre) permitía huecos (ej. banner 1 y banner 4, sin 2 ni 3) y no daba ninguna forma visual de saber qué banner era cuál para intercambiar posiciones. **Planeado y aprobado antes de ejecutar** (dos rondas: alcance general, luego elegir entre navegar a la edición estándar de Strapi vs. reconstruir un editor inline al hacer clic en un banner — se eligió la primera opción, menor esfuerzo y cero riesgo de duplicar lógica ya construida). **Solución:** `order` se quita del formulario de creación/edición (`ensureBannerViewConfiguration.ts`, manipula `layouts.edit` de la configuración del Content Manager) y se asigna solo al final (`max(order)+1`) vía lifecycle `beforeCreate` (`lifecycles/defaultBannerOrder.ts`) — coexiste con el hook reactivo de T-09 (`recompactBannersOnChange.ts`) sin conflicto, ambos convergen al mismo resultado (1..N contiguo). Nueva página "Homepage banners" en el admin (`app.addMenuLink`, API pública de Strapi) con la lista de banners arrastrable (`@dnd-kit`, agregado como dependencia explícita aunque ya estaba disponible transitivamente vía el propio admin de Strapi) — al soltar, llama a un endpoint propio autenticado con la sesión de admin (`POST /banners/reorder`, registrado vía `strapi.server.routes({type:'admin', ...})`, fuera de `/api` para que no sea alcanzable con un content-api token) que renumera 1..N; se extrajo el helper de renumeración contigua para compartirlo con la recompactación de T-09. **Bug real encontrado y corregido:** las miniaturas de la nueva página salían vacías a pesar de que los medios sí estaban correctamente subidos (confirmado inspeccionando la BD directo) — causa: el `find` del Content Manager (que se usó primero para listar) limita el populate a un nivel (`populateDeep(1)`); para un dynamiczone eso alcanza al componente (`banner.image-content`/`video-content`) pero no a los medios *dentro* del componente (un nivel más abajo), sin importar qué se pidiera por query. Se resolvió con un endpoint de listado propio (`GET /banners/list`) que usa el Document Service directo (`strapi.documents(uid).findMany()`), sin ese límite — de paso corrigió un bug menor de estado (el badge "Publicado/Borrador" leía el `publishedAt` de la versión *borrador* del documento, que siempre es `null`; ahora compara contra el set de `documentId` con versión publicada real). **Verificado en vivo por el programador:** miniatura visible (imagen/póster de escritorio, no el de celular — ajuste pedido después), badge correcto, drag & drop reordena y persiste, banner nuevo cae al final sin pedir número. |
| *(fuera de plan)* | Bug de producción: CSS de marca del admin no cargaba fuera de `strapi develop` | Claude Code | 2026-08-03 | Encontrado al desplegar las dos mejoras anteriores a QA: el programador reportó que el tema visual (gradiente de fondo, logo distinto en dark, subtítulo del login oculto — todo trabajo de UI fuera de plan de rondas anteriores) no se veía en QA, aunque sí en local. **Diagnóstico:** se confirmó con DevTools (pestaña Network, filtro `.css`, recarga forzada) que el navegador **nunca pedía ningún archivo `.css`** en QA — no era caché ni un problema del deploy. Causa raíz: `background.css` se importaba directo (`import './background.css'`) en `src/admin/app.tsx`; Vite separa ese import en un chunk de CSS aparte que solo se inyecta cuando el manifiesto de precarga de alguna ruta lazy-loaded lo dispara en tiempo de ejecución — y en este proyecto ese disparador nunca ocurría, en ningún build de producción (reproducido también en local con `strapi build && strapi start`, no solo en QA). **Solución:** se reemplazó el `import` de CSS por un `<style>` inyectado a mano en `bootstrap()` (`injectBrandStyles.ts`), mismo patrón ya usado para el tema claro/oscuro y el título de pestaña — deja de depender del bundler para algo que tiene que funcionar siempre. Los logos referenciados dentro del CSS (`brick-logo-dark.png`, `favicon-dark.png`) se importan como módulos para que Vite les siga dando su URL final con hash, en vez de rutas relativas literales (que ya no se resuelven al inyectar el CSS como texto plano). **Verificado:** el string del CSS ahora aparece embebido directo en el bundle principal (el que carga siempre, sin depender de ningún chunk lazy) tanto en local como en QA tras redesplegar; el programador confirmó visualmente que el gradiente, el logo dark y el subtítulo oculto ya se ven bien en QA. **Incidente menor durante el redeploy:** al repetir los pasos de actualización en QA, se corrieron `git pull` y `restart` sin los `npm ci`/`npm run build` intermedios — el servicio seguía sirviendo el build viejo (confirmado comparando el hash del archivo JS servido antes/después); se corrigió repitiendo la secuencia completa. |
| T-14 | Editor de bloques del cuerpo del artículo (embeds YouTube/redes) | Claude Code | 2026-07-27 | **Intento 1 (fallido): bloque personalizado dentro del editor de Bloques nativo.** Se registró un bloque "Embed" vía la API pública `addRichTextBlocks` (`@strapi/content-manager`, solo admin) — funcionaba visualmente en el editor, pero **el servidor rechazaba el guardado** con `"Block node is of invalid type"`. Causa raíz confirmada leyendo el código fuente real de Strapi: `blocks-validator.js` valida el campo `blocks` contra un `switch` cerrado de 6 tipos (`paragraph/heading/quote/list/image/code`), sin ningún mecanismo de extensión público ni oculto — cualquier tipo fuera de esa lista falla siempre. Se descartó ese enfoque por completo (se revirtieron `src/admin/app.tsx`, el CSP de `frame-src` y el `referrerPolicy`, ambos añadidos solo para esa prueba). **Intento 2 (exitoso): Dynamic Zone.** Se rediseñó `content` de Article como Dynamic Zone de 6 componentes nuevos (`src/components/article/`: `paragraph`, `heading`, `quote`, `image`, `list`, `list-item`, `embed`) — mismo patrón ya aprobado en Banner/T-07, sin límite de tipos porque los componentes de una Dynamic Zone no pasan por ese validador cerrado. `article.embed` solo pide una URL; el proveedor (YouTube/Instagram/Twitter-X/TikTok/Facebook) se detecta por patrón de la URL al momento de renderizar (pendiente implementar esa misma detección en el sitio, T-15), no al guardar. **"Nota al pie" descartada** del alcance por decisión del programador (no existe hoy ni en el sitio ni en el CMS; se aproxima a mano con texto si algún post la necesita). **Bonus:** al ya no depender del editor nativo, `article.quote` sí pudo llevar un campo `author` opcional real (más fiel al `<cite>` que ya soporta `BlogPostLayout.tsx`), revirtiendo la decisión anterior de "autor como texto libre". **Verificado en vivo:** el programador insertó los 6 tipos de bloque (incluido un Embed con una URL de YouTube real) y guardó sin error — confirmado también en BD (tabla `articles_cmps` con los 6 componentes en el orden correcto, `components_article_embeds.url` con el valor exacto). |

---

## Tareas en progreso 🟡

| ID | Tarea | Responsable | Iniciada | Notas |
|---|---|---|---|---|
| | | | | |

---

## Tareas pendientes ⏳

| ID | Tarea | Bloqueada por (si aplica) |
|---|---|---|
| T-16 | Content type StaticText (P3) | |
| T-17 | Consumo de textos estáticos en el sitio (P3) | |
| T-18 | Manejo de errores y observabilidad | |
| T-19 | Despliegue final y monitoreo de facturación | |

---

## Tareas bloqueadas 🔴

| ID | Tarea | Motivo del bloqueo | Quién debe resolverlo |
|---|---|---|---|
| T-05 (verificación real) | Verificar `deploy/nginx.conf` + `deploy/strapi.service` en una instancia real | La instancia EC2 no existe aún | Alexis Herrera — solicitud de aprovisionamiento enviada 2026-07-20 (specs: Ubuntu, puertos 80/443/22, Postgres solo local; pendiente confirmar consola AWS, qa/prod separadas o compartidas, y dominio) |

---

## Decisiones tomadas durante la ejecución

| Decisión | Justificación | Impacto |
|---|---|---|
| Commit inicial de bootstrap directo en `main` (README + `.gitignore`) | El repo se creó completamente vacío; no puede existir `develop`/`pre-qa`/`qa` sin al menos un commit del que derivarlas. Es la única excepción a la regla de "nunca commits directos a main". | Ninguno — `main` queda con solo el bootstrap; todo el desarrollo real ocurre en la rama funcional. |
| Modelo de ejecución: Sonnet 5 (no Opus) | El workflow `ejecutar-plan.md` especifica siempre modelo Sonnet para la fase de ejecución (Opus es solo para generar el plan). Confirmado con el programador. | Ninguno funcional; solo trazabilidad de qué modelo generó el código. |
| PostgreSQL local vía Postgres.app (no Homebrew/Docker) | La máquina no tenía Homebrew, Docker ni Postgres instalados. Postgres.app no requiere permisos de administrador; se inicializó y arrancó el servidor por línea de comandos (`initdb`/`pg_ctl`) sin abrir la GUI. | Desarrollo local requiere que el desarrollador arranque el servidor manualmente (`pg_ctl start`) — no es un servicio del sistema. Documentar en onboarding si otro compañero retoma. |
| Scaffold de Strapi generado en carpeta temporal y movido al repo | `create-strapi-app` exige un directorio vacío; el repo ya tenía `.git`, `.gitignore` y `README.md` del bootstrap. Se generó en `/tmp` y se fusionó preservando `.git` y el README del proyecto. | Ninguno — resultado final idéntico a un scaffold directo. |
| Docker Desktop instalado por el programador durante la ejecución | No había runtime Docker disponible al iniciar T-05; el programador lo instaló para poder verificar el Dockerfile localmente en vez de dejarlo como bloqueo. | T-05 quedó verificado (build + run + health check) en vez de solo documentado. |
| S3 (T-03): código completo, verificación real diferida | No existe aún bucket S3 ni usuario IAM dedicado. Se decidió no usar credenciales personales/admin del programador (regla de mínimo privilegio de `infraestructura.md`) — se solicitará un usuario IAM dedicado con permisos acotados al bucket. | La tarea T-03 se considera completa en código; la subida de prueba real queda como tarea bloqueada explícita (ver arriba), no como pendiente silenciosa. |
| **Cambio de arquitectura de despliegue: EC2+Nginx+systemd en vez de Docker/ECS+Fargate** | El programador confirmó (2026-07-17) que se usará una instancia EC2 con Nginx y PostgreSQL local, sin Docker. Es una excepción explícita a los defaults de Engine (`infraestructura.md`: "ECS+Fargate para todos los nuevos desarrollos"; `arquitectura.md` marca el patrón app+BD en una sola instancia como monolítico, no recomendado para proyectos nuevos). Aceptada como decisión de infraestructura del cliente. | Se retiró el trabajo de T-05 basado en Docker (ya commiteado y verificado) y se rehizo con Nginx/systemd. Ver riesgo nuevo en `PLAN.md` §11 (app+BD en la misma instancia, sin RDS → backups manuales). |
| **PostgreSQL sin RDS: backups manuales** | Consecuencia directa de correr Postgres en la misma EC2 que la app. AWS no gestiona backups/HA en este esquema. | Documentado en `deploy/README.md` §3: mínimo, `pg_dump` programado. Pendiente de implementar cuando exista la instancia. |
| **Un solo usuario IAM con acceso a ambos buckets S3 (prod y qa)** | El programador confirmó que las credenciales entregadas son de un usuario con acceso a los dos buckets, no separado por ambiente. | Riesgo aceptado y documentado (`PLAN.md` §11); no bloquea el desarrollo. Recomendable separar en el futuro. |
| **`ACL: undefined` explícito en `config/plugins.ts`** | El bucket usa "Bucket owner enforced" (ACLs deshabilitadas, default de S3 desde abril 2023). El provider de Strapi intenta mandar `ACL: public-read` salvo que se le indique lo contrario, y S3 rechaza cualquier header de ACL en estos buckets con `AccessControlListNotSupported`. | La subida a S3 ahora funciona (verificado). Como efecto secundario, `isPrivate()` del provider (que depende de `ACL === 'private'`) nunca puede ser `true` en este tipo de bucket — el control de acceso público/privado queda 100% en manos de la bucket policy, no de Strapi. |
| **Modelo de publicación confirmado: Draft & Publish + webhook** | El programador confirmó (2026-07-17) la recomendación ya documentada en `PLAN.md` §3, cerrando la pregunta abierta del PRD §13. | Desbloquea T-10 sin necesidad de validación adicional del equipo. |
| **Rama base del sitio confirmada: `dev`** | El programador confirmó (2026-07-17) que, como `autoexplora-alfa` no tiene `develop`, los cambios de integración (T-11, T-15, T-17) se basan en `dev` — excepción al flujo estándar de Engine, ya documentada en `PLAN.md` §12. | Desbloquea T-11 sin necesidad de validación adicional del equipo del sitio. |
| **T-06: RBAC nativo del Admin Panel en vez del plugin Users & Permissions** | El plan original decía "plugin Users & Permissions", pero ese plugin es para autenticación de usuarios finales de un sitio público — no aplica a quien entra al admin de Strapi. El sistema correcto es el RBAC nativo (Settings > Roles), usando el rol "Editor" que Strapi trae de fábrica. | Corrección de alcance sin impacto en el criterio de aceptación (login de editor con permisos acotados) — solo cambia el mecanismo técnico. |
| **T-07: Banner rediseñado con Dynamic Zone (Imagen/Video) tras prueba manual** | Al probar el formulario, el programador identificó 3 problemas de producto: (1) el campo "sección" era redundante (un solo carrusel en home, no multi-sitio); (2) el póster aparecía aunque no se hubiera elegido tipo de media, sin flujo progresivo; (3) faltaba soporte de versiones desktop/mobile separadas. Se resolvió con una Dynamic Zone de dos Componentes (Imagen/Video), cada uno con sus propios campos desktop/mobile — logra la UX progresiva pedida usando UI nativa de Strapi, sin construir un formulario custom (que sí hubiera arriesgado el deadline). | Cambia el modelo de datos de Banner respecto al PRD original (RF-03 decía "por sección"); documentado como decisión de producto del programador. Bonus: el póster obligatorio en video (RF-04) ahora se cumple nativamente vía `required: true` en el componente, sin necesitar lifecycle hook para esa parte — T-08 se reduce a validar formato/peso de archivo. |
| **`altText` propio eliminado de los componentes de Banner** | El programador notó que Strapi ya provee "texto alternativo" nativo por archivo en la Media Library — el campo custom era redundante. | Ninguno — simplificación, sin pérdida de funcionalidad. |
| **Bucket policy de lectura pública aplicada (Alexis Herrera, 2026-07-17)** | Tras la bucket policy inicial, seguía dando `403` porque nunca se había guardado ninguna policy (confirmado con "No hay ninguna política que mostrar" en la consola). Se le compartió el JSON exacto de policy pública (`s3:GetObject`) para ambos buckets; tras aplicarla, verificado con `curl` → `200 OK`. | T-03 queda 100% cerrado, incluyendo lectura pública — ya no hay bloqueo pendiente de S3. |
| **T-08: validación como hook global, no por content type** | RF-11 describe la validación de formato/peso como aplicable a "cada subida", no específica de Banner. Se implementó vía `strapi.db.lifecycles.subscribe` sobre `plugin::upload.file` en vez de un lifecycle propio de Banner. | Cubre automáticamente Article (T-13) y StaticText (T-16) sin duplicar lógica cuando se construyan esos content types. |
| ✅ **CORS resuelto en ambos buckets S3 (Alexis Herrera, 2026-07-20)** | Al probar T-08 no se veían las miniaturas en la Media Library aunque las URLs eran públicas. Se diagnosticó falta de configuración CORS y se compartió el JSON a Alexis. Aplicado y verificado el mismo día: `GET` con header `Origin` devuelve `Access-Control-Allow-Origin: *` en `govirtual-autoexplora-cms-qa` y `-prod`. | Necesario pero **no suficiente** — ver siguiente hallazgo. Nota técnica: verificar CORS con `curl -I` (HEAD) da falso negativo — S3 solo devuelve el header en `GET`/`OPTIONS` reales, no en `HEAD`. |
| ✅ **Causa real de las miniaturas rotas: Content Security Policy de Strapi, no CORS** | Tras el fix de CORS, las miniaturas seguían rotas. La consola del navegador mostró el error real: `img-src`/`media-src 'self' data: blob:` bloqueaba cualquier carga desde el bucket S3 — es el CSP por default de Strapi (middleware `strapi::security`), no un problema de AWS. Se corrigió en `config/middlewares.ts`, agregando ambos hosts S3 (qa y prod) a `img-src`/`media-src`. | T-03 (miniaturas) queda 100% cerrado — verificado visualmente por el programador tras el fix. CORS y CSP eran ajustes **complementarios**, ambos necesarios; no hay que revertir el de CORS. |
| **T-13: Article diseñado a partir del blog real de `autoexplora-alfa`, no solo del PRD** | El programador pidió explícitamente revisar cómo está construido un post de blog hoy en el sitio antes de diseñar el schema, para preservar el diseño/estructura actual en vez de reinventarlo. | El schema de Article (título, slug, autor, hero, contenido, descripción, resumen) refleja lo que el sitio realmente necesita renderizar, no una interpretación genérica de RF-08. |
| **T-13: sin `Category`/`Tag`** | El programador confirmó que los posts de blog no están categorizados por ahora. | Se elimina esa parte del alcance original de T-13 sin afectar el criterio de aceptación (CRUD + Draft & Publish); se puede agregar después como campo adicional si se necesita. |
| **T-13: `hero` como campo de imagen manual e independiente (no automático, no video)** | Discusión iterativa con el programador: primero se consideró derivar el hero automáticamente de la primera imagen del cuerpo del post; el programador decidió que fuera un campo separado que el editor sube a mano, para que el hero banner del post y la miniatura de la card en `/blog` no dependan de si el post lleva o no imágenes en el cuerpo. | Simplifica la carga de trabajo de quien redacta (solo sube una imagen) y desacopla el hero de la validación/estructura del cuerpo (bloques). |
| **T-13/T-14: editor de bloques nativo de Strapi en vez de CKEditor** *(superada 2026-07-27, ver fila siguiente)* | El programador confirmó ir con la opción recomendada: bloques nativos (JSON estructurado) en vez del plugin CKEditor (HTML crudo), por seguridad y para no depender de un plugin de terceros. | El campo `content` inició como `type: "blocks"`, pero se rediseñó a Dynamic Zone al implementar T-14 — ver hallazgo abajo. La motivación original (JSON estructurado, sin dependencia de terceros) se mantiene igual con Dynamic Zone. |
| **T-13/T-14: autor de una quote como texto, sin campo dedicado** *(revertida 2026-07-27, ver fila siguiente)* | El programador confirmó que basta con que quien redacta escriba el nombre del autor como parte del texto de la cita — no se necesita un bloque de Strapi personalizado para eso. | Válida mientras se usó el editor de Bloques nativo (evitaba construir un Custom Block solo para esto). Al pivotear a Dynamic Zone, se revirtió: `article.quote` sí lleva un campo `author` opcional real. |
| 🔴 **T-14: el editor de Bloques nativo NO puede guardar tipos de nodo personalizados — límite duro del framework** | Se implementó un bloque "Embed" (YouTube/redes) registrado vía `addRichTextBlocks` (API pública, pero **solo del lado admin**, `@strapi/content-manager`). Se veía y funcionaba bien en el editor, pero al guardar el servidor devolvía `"Block node is of invalid type"`. Se leyó el código fuente real de Strapi (`@strapi/core/dist/services/entity-validator/blocks-validator.js`): el campo `blocks` valida cada nodo con un `switch(value.type)` **cerrado** de 6 casos (`paragraph/heading/quote/list/image/code`) — cualquier otro tipo cae al `default`, que siempre falla (`yup.mixed().test(() => false)`). Se verificaron las 3 rutas de extensión posibles (`strapi.customFields` — `blocks` no está en su `ALLOWED_TYPES`; `strapi.validators` registry — no tiene relación con `entity-validator`; el propio módulo `entityValidator` — sin ningún gancho de extensión). **No existe ninguna API pública ni oculta para extender ese enum.** Solo sería posible con un parche no soportado (`patch-package`), que se perdería en cada actualización de Strapi y probablemente rompería también la validación de esquema del lado del editor. | Se abandonó por completo el enfoque de "bloque personalizado dentro de `blocks`". Se revirtieron `src/admin/app.tsx` (registro `addRichTextBlocks`), `src/admin/extensions/blocks/` (archivos `Embed.tsx`/`embedProvider.ts`, eliminados) y `config/middlewares.ts` (CSP `frame-src`, `referrerPolicy` — ambos añadidos solo para el preview de YouTube en ese intento). Precedente importante para cualquier trabajo futuro con el campo `blocks` de Strapi: **el set de tipos de nodo es fijo y cerrado en el servidor**, sin importar lo que el admin permita registrar visualmente. |
| **T-14: pivote a Dynamic Zone para `content` de Article** | Consecuencia directa del hallazgo anterior. Se le presentó al programador la disyuntiva (Dynamic Zone vs. descartar el embed) y confirmó ir con Dynamic Zone. | `content` de Article pasó de `type: "blocks"` a `type: "dynamiczone"` con 6 componentes nuevos en `src/components/article/` (`paragraph`, `heading`, `quote`, `image`, `list`, `list-item`, `embed`) — mismo patrón ya aprobado en Banner/T-07. Sin límite de tipos (los componentes de una Dynamic Zone no pasan por `blocks-validator.js`). El artículo de prueba creado en T-13 perdió su contenido de prueba (tipo `blocks` incompatible con la nueva estructura) — sin impacto real, era solo un post de prueba. Verificado en vivo: el programador guardó un artículo con los 6 tipos de bloque, incluido un Embed de YouTube real, sin error — confirmado también en BD. |
| **T-14: proveedor del embed se detecta por patrón de URL, sin campo de selección manual** | El programador confirmó la opción recomendada: un solo campo `url` en `article.embed`; el proveedor (YouTube/Instagram/Twitter-X/TikTok/Facebook) se detecta por el hostname de la URL, sin pedirle a quien redacta que lo elija de una lista. | Cero configuración extra para el editor. La detección debe implementarse igual en el sitio (T-15) para decidir cómo renderizar cada proveedor — YouTube tiene URL de iframe pública y estable (`youtube.com/embed/{id}`); el resto (sin oEmbed implementado) se resuelve como tarjeta con enlace hasta que se justifique más trabajo. |
| **T-14: "nota al pie" descartada del alcance** | El programador confirmó la opción recomendada: no construir ningún mecanismo dedicado (ni bloque personalizado ni numeración automática) — no existe hoy ni en el sitio ni en el CMS, y no hay precedente del cual partir. | Si algún post lo necesita, quien redacta lo aproxima a mano con texto (ej. "dato importante (1)" + un párrafo final "(1) Fuente"). Cero trabajo de ingeniería adicional; mismo criterio ya usado para el autor de las quotes en la decisión original (aunque esa se revirtió después). |
| **T-15: mergear `dev` (ya sincronizada con `post-lanzamiento`) hacia la rama del CMS antes de tocar código** | `autoexplora-alfa` ya se había lanzado a producción y `post-lanzamiento` había acumulado varios commits ajenos al CMS desde que se creó `dev` para T-11 (mediados de julio). El programador propuso mergear `post-lanzamiento` a `dev`; se confirmó que ya estaba hecho (0 commits de diferencia). | Se trajo `dev` actualizada a `feature/PJ5040-cms-autoexplora-mvp` con un merge limpio (sin conflictos), evitando construir T-15 sobre una base de código que ya no coincide con lo que corre en producción. Costo único, sin tocar la rama compartida `dev` directamente. |
| 🔴 **T-15: `populate[hero]=*` falla en un campo `media` simple (Strapi 5.50.2)** | Al construir el fetch de Article, se copió el mismo patrón `=*` usado para los componentes de la Dynamic Zone de Banner/Article — pero aplicado al campo `hero` (relación directa, no dentro de un componente), Strapi devuelve `400 ValidationError: Invalid key related at hero.related`. Se aisló con curl directo contra la API antes de tocar código de Next.js. | Se usa `populate[hero]=true` en vez de `=*` para cualquier campo `media` de nivel superior. Los componentes dentro de una Dynamic Zone (`populate[content][on][...][populate]=*`) no tienen este problema — el bug es específico de relaciones simples en el nivel raíz del content type. Precedente útil para cualquier fetch futuro de un campo media directo en Strapi 5. |
| 🔴 **T-15: `publishedAt` siempre es `null` en `status=draft`, rompiendo el orden "más reciente primero"** | Se detectó en vivo: un artículo nunca publicado apareció ANTES que uno publicado el mismo día, en el listado `/blog` en modo preview. Causa: `status=draft` devuelve siempre la versión borrador de cada entrada (por diseño de Strapi — así es como distingue "borrador" de "publicado"), y esa versión nunca tiene `publishedAt`, sin importar si la misma entrada ya se publicó en otra ocasión. El `sort=publishedAt:desc` de la API por tanto no ordena nada ahí (todo empata en `null`). | Se usa `createdAt` como respaldo de fecha (`publishedAt ?? createdAt`) y se reordena en el cliente (`fetchBlogPosts()` en `lib/server/strapiApi.ts`) con esa fecha efectiva, en vez de confiar en el `sort` de la API. Inofensivo en producción (`status=published`), donde `publishedAt` siempre es real. |
| **T-15: permisos del token de solo lectura del sitio, automatizados por bootstrap** | El token `autoexplora-alfa-site-read` (creado manualmente para T-11) solo tenía permisos de Banner; T-15 necesitaba también Article. | En vez de pedir un paso manual por ambiente (dev/qa/prod), se creó `src/bootstrap/ensureSiteApiTokenPermissions.ts` en el CMS — idempotente, mismo patrón que `ensureEditorPermissions.ts` — que le agrega los permisos faltantes en cada arranque de Strapi. Si el token aún no existe en un ambiente (debe crearse una vez, a mano), el bootstrap solo advierte y no falla. |
| **`publicationDate` de Article: Custom Field en vez de un campo `date` nativo** | Se necesitaba que el campo autorrellenara "hoy" al crear (sin bloquear el guardado por ser `required` y estar vacío del lado del cliente) pero quedara 100% editable, incluso a una fecha pasada — ningún mecanismo nativo de Strapi (`default` estático, lifecycle `beforeCreate`) resuelve el autorrelleno sin que la validación Yup del admin bloquee antes de llegar al servidor. | Precedente reusable: cualquier campo futuro que necesite "un valor por default calculado en el momento, no un literal fijo" en el formulario de creación necesita un Custom Field que lo autorrellene del lado del cliente al montar — un `default` de schema o un lifecycle del servidor no alcanzan. |
| 🔴 **El `find` del Content Manager limita el populate a un nivel (`populateDeep(1)`) — no sirve para leer medios dentro de un dynamiczone** | Al construir la página "Homepage banners", se usó primero el endpoint estándar del Content Manager (`GET /content-manager/collection-types/:model`) para listar — las miniaturas salían vacías sin importar qué `populate` se mandara por query. Se leyó el código fuente real (`@strapi/content-manager/.../services/utils/populate.js`): el controlador `find` encadena `.populateDeep(1)`, y `getPopulateForDZ` (el resolver de dynamiczones) **ignora por completo** cualquier populate manual pasado por query para sus componentes — siempre genera el suyo, limitado al `maxLevel` configurado. Para un dynamiczone, el componente en sí ya consume el nivel 1; sus propios campos de media quedan en el nivel 2, que nunca se alcanza. | Cualquier vista custom del admin que necesite leer datos anidados dentro de un dynamiczone (más allá de lo que el Content Manager expone por defecto) debe construir su propio endpoint admin-autenticado sobre el Document Service (`strapi.documents(uid).findMany()`), no reusar el `find` del Content Manager — ese endpoint está optimizado para lo que la UI estándar de Strapi necesita, no como API general. |
| 🔴 **CSS importado directo en `src/admin/app.tsx` nunca se carga en un build de producción** | Vite separa un `import './archivo.css'` hecho desde `app.tsx` en un chunk de CSS aparte, que solo se inyecta en el DOM cuando el manifiesto de precarga (`__vite__mapDeps`) de alguna ruta lazy-loaded lo dispara en tiempo de ejecución. En este proyecto ese disparo nunca ocurre — confirmado con DevTools (cero requests a `.css`) y reproducido en local con `strapi build && strapi start`, no solo en QA. `strapi develop` nunca lo expone porque Vite dev sirve cada import de CSS como `<style>` vía HMR incondicionalmente. | Cualquier CSS que dependa de `app.tsx`/`bootstrap()` para verse SIEMPRE (branding, overrides globales) debe inyectarse por JavaScript (`document.createElement('style')`) dentro de `bootstrap()`, no por `import` de un archivo `.css` — ver `injectBrandStyles.ts`. Un `import './x.css'` de Vite en este punto de la app solo es confiable en modo desarrollo. |
| **Permisos de `/usr/local/qa-autoexplora-cms` tras el clon inicial: `ubuntu` no podía escribir, `strapi` no tenía acceso a GitHub** | El clon inicial (T-19) se hizo con el acceso a GitHub del usuario `ubuntu`, y luego el directorio completo se le pasó de dueño a `strapi` (el usuario de sistema, `shell: nologin`, que corre el servicio) — pero `strapi` nunca tuvo su propia llave SSH para GitHub. Al intentar actualizar (2026-08-03), ni `ubuntu` (sin permiso de escritura) ni `strapi` (sin acceso a GitHub, y sin poder abrir sesión interactiva por el `nologin`) podían hacer `git pull`. | Se corrigió con `sudo chown -R ubuntu:strapi /usr/local/qa-autoexplora-cms` — dueño `ubuntu` (para poder `git pull`/`npm ci`/`npm run build` con su propio acceso a GitHub), grupo `strapi` sin cambios (para que el servicio, que lee vía permisos de grupo, siga funcionando igual). Repetir este ajuste si se vuelve a clonar desde cero en una instancia nueva (incluyendo producción). |

---

## Archivos creados o modificados

| Archivo | Tipo de cambio | Tarea relacionada |
|---|---|---|
| `README.md`, `.gitignore` | Modificado (bootstrap fusionado con scaffold) | T-01 |
| `package.json`, `package-lock.json`, `tsconfig.json`, `favicon.png` | Creado | T-01 |
| `config/database.ts`, `config/server.ts` | Creado | T-01/T-02 |
| `config/admin.ts`, `config/api.ts`, `config/middlewares.ts` | Creado | T-01 |
| `config/plugins.ts` | Creado, luego modificado (`ACL: undefined` — fix de subida) | T-03 |
| `.env.example` | Creado (vars de BD y S3) | T-02/T-03 |
| `src/`, `database/migrations/.gitkeep`, `public/`, `types/` | Creado | T-01 |
| `CLAUDE.md` | Creado, luego modificado (sección Deployment) | T-04 / rework T-05 |
| `Dockerfile`, `.dockerignore`, `deploy/task-definition.json` | Creado y luego **eliminado** (cambio de arquitectura) | T-05 → rework |
| `deploy/nginx.conf`, `deploy/strapi.service` | Creado | Rework T-05 |
| `deploy/README.md` | Creado, luego reescrito (EC2 en vez de ECS) | T-05 → rework |
| `.env.example` | Modificado (buckets S3 reales) | Rework T-05 |
| `src/api/banner/` (schema, controller, service, routes) | Creado | T-07 |
| `src/components/banner/image-content.json`, `video-content.json` | Creado, luego modificado (se quitó `altText`) | T-07 |
| `src/bootstrap/ensureEditorPermissions.ts` | Creado (idempotente, con auto-actualización de campos) | T-06 |
| `src/index.ts` | Modificado (conecta el bootstrap, luego el hook de validación) | T-06 / T-08 |
| `src/lifecycles/validateUploadedFiles.ts` | Creado | T-08 |
| `src/api/banner/services/reorder.ts` | Creado | T-09 |
| `config/cron-tasks.ts` | Creado | T-09 |
| `src/lifecycles/recompactBannersOnChange.ts` | Creado | T-09 |
| `config/server.ts` | Modificado (cron habilitado) | T-09 |
| `src/bootstrap/ensurePublicationWebhook.ts` | Creado | T-10 |
| `config/custom.ts` | Creado (`SITE_REVALIDATE_WEBHOOK_URL`/`PREVIEW_WEBHOOK_SECRET`) | T-10 |
| `config/middlewares.ts` | Modificado (CSP: hosts S3 en `img-src`/`media-src`) | T-03 (fix) |
| **`autoexplora-alfa`**: `lib/server/strapiApi.ts` | Creado | T-11 |
| **`autoexplora-alfa`**: `app/page.tsx` | Modificado (fetch server-side de banners) | T-11 |
| **`autoexplora-alfa`**: `app/components/HomeCarousel.tsx` | Modificado (array hardcodeado → prop `slides`; fix LCP) | T-11 |
| **`autoexplora-alfa`**: `next.config.ts` | Modificado (hosts S3 del CMS en `remotePatterns`) | T-11 |
| **`autoexplora-alfa`**: `.env.example`/`.env` | Modificado (`STRAPI_API_URL`/`STRAPI_API_TOKEN`/`STRAPI_PUBLICATION_STATUS`) | T-11 |
| `src/api/audit-log/` (schema, controller, service, routes) | Creado | T-12 |
| `src/lifecycles/auditLog.ts` | Creado | T-12 |
| `src/admin/app.tsx` (activado desde `.example.tsx`), `src/admin/extensions/brick-logo.png`, `src/admin/assets.d.ts` | Creado | UI (fuera de plan) |
| `src/bootstrap/ensureSpanishLabels.ts` | Creado | UI (fuera de plan) |
| `src/api/article/` (schema, controller, service, routes) | Creado | T-13 |
| `src/bootstrap/ensureEditorPermissions.ts` | Modificado (agrega Article a `EDITOR_MANAGED_CONTENT_TYPES`) | T-13 |
| `src/lifecycles/auditLog.ts` | Modificado (agrega `api::article.article` a `TRACKED_UIDS`) | T-13 |
| `src/bootstrap/ensureSpanishLabels.ts` | Modificado (agrega `ARTICLE_FIELDS`, incluida nota de ayuda sobre el botón "Regenerar" del slug) | T-13 |
| `src/admin/extensions/blocks/Embed.tsx`, `embedProvider.ts` | Creado y luego **eliminado** (bloque personalizado dentro de `blocks`, descartado por límite del framework) | T-14 → rework |
| `src/admin/app.tsx` | Modificado (registro `addRichTextBlocks`) y luego **revertido** a su estado de UI (fuera de plan) | T-14 → rework |
| `config/middlewares.ts` | Modificado (CSP `frame-src`, `referrerPolicy`) y luego **revertido** | T-14 → rework |
| `src/api/article/content-types/article/schema.json` | Modificado (`content`: `blocks` → `dynamiczone` de 6 componentes) | T-14 |
| `src/components/article/paragraph.json`, `heading.json`, `quote.json`, `image.json`, `list.json`, `list-item.json`, `embed.json` | Creado | T-14 |
| `src/bootstrap/ensureSpanishLabels.ts` | Modificado (etiquetas en español para los 7 componentes nuevos de `article.*`) | T-14 |
| `src/bootstrap/ensureSiteApiTokenPermissions.ts` | Creado (idempotente) | T-15 |
| `src/index.ts` | Modificado (conecta el nuevo bootstrap) | T-15 |
| **`autoexplora-alfa`**: `app/blog/data/posts.ts` | Modificado (se elimina el mock `POSTS`/`getPost`; se agrega el tipo `embed` a `ContentBlock` y `publishedAtISO`/`description` a `Post`) | T-15 |
| **`autoexplora-alfa`**: `lib/server/strapiApi.ts` | Modificado (agrega `fetchBlogPosts`, `fetchBlogPostBySlug`, `fetchBlogSlugs` y el mapper Article → Post) | T-15 |
| **`autoexplora-alfa`**: `lib/blog/embedProvider.ts` | Creado (detección de proveedor de embed, lado del sitio) | T-15 |
| **`autoexplora-alfa`**: `app/blog/page.tsx` | Modificado (listado real vía `fetchBlogPosts`, `PostCard` sin cambios) | T-15 |
| **`autoexplora-alfa`**: `app/blog/[slug]/page.tsx` | Modificado (detalle real vía `fetchBlogPostBySlug`/`fetchBlogSlugs`; SEO completado con `description`/`datePublished`) | T-15 |
| **`autoexplora-alfa`**: `app/components/BlogPostLayout.tsx` | Modificado (agrega el caso `embed`: iframe de YouTube o tarjeta con enlace) | T-15 |
| **`autoexplora-alfa`**: `app/sitemap.ts` | Modificado (`blogUrls` async, slugs reales) | T-15 |
| **`autoexplora-alfa`**: `.env.example` | Modificado (comentario generalizado a "banners y blog") | T-15 |
| `favicon.png` | Modificado (ícono de marca, reemplaza el genérico de Strapi) | UI (fuera de plan) |
| `favicon-dark.png` | Creado (versión clara del ícono, para tema oscuro) | UI (fuera de plan) |
| `src/admin/extensions/brick-logo-dark.png` | Creado (versión blanca del logo horizontal, para tema oscuro) | UI (fuera de plan) |
| `src/admin/extensions/background.css` | Creado (gradiente de fondo y logos por tema, ajustes de espaciado/copy del login) | UI (fuera de plan) |
| `src/admin/extensions/theme.ts` | Creado (detección de tema claro/oscuro activo, sin API oficial de Strapi) | UI (fuera de plan) |
| `src/admin/extensions/pageTitle.ts` | Creado (títulos de pestaña con marca "Brick \| ...") | UI (fuera de plan) |
| `src/admin/app.tsx` | Modificado (logo del menú → favicon, escalas de color claro/oscuro, traducciones del login, llamadas a los nuevos módulos) | UI (fuera de plan) |
| `src/admin/assets.d.ts` | Modificado (declaración de módulo para imports `*.css`) | UI (fuera de plan) |
| `src/bootstrap/ensureEditorPermissions.ts` | Modificado (traduce la descripción del rol Editor) | UI (fuera de plan) |
| `src/bootstrap/ensureSpanishLabels.ts` | Modificado (descripciones de ayuda faltantes) | UI (fuera de plan) |
| `src/components/article/*.json`, `src/components/banner/{image,video}-content.json` | Modificado (agrega `info.icon` a cada componente) | UI (fuera de plan) |
| `src/admin/extensions/TodayDateInput.tsx` | Creado (Custom Field: DatePicker con autorrelleno de "hoy") | Fecha editorial (fuera de plan) |
| `src/admin/app.tsx` | Modificado (registra el Custom Field `todayDate`; luego, mismo commit de banners, agrega `app.addMenuLink` para "Homepage banners"; luego, commit de fix de CSS, quita el `import './background.css'` y llama `injectBrandStyles()`) | Fecha editorial / Banners (fuera de plan) |
| `src/api/article/content-types/article/schema.json` | Modificado (agrega `publicationDate`, `customField: global::todayDate`) | Fecha editorial (fuera de plan) |
| `src/bootstrap/ensureEditorPermissions.ts` | Modificado (agrega `publicationDate` a los campos gestionables de Article) | Fecha editorial (fuera de plan) |
| `src/bootstrap/ensureSpanishLabels.ts` | Modificado (etiqueta "Fecha de publicación" para `publicationDate`) | Fecha editorial (fuera de plan) |
| `types/generated/contentTypes.d.ts` | Modificado (regenerado por Strapi) | Fecha editorial (fuera de plan) |
| **`autoexplora-alfa`**: `lib/server/strapiApi.ts` | Modificado (quita el fallback `publishedAt ?? createdAt`, usa `publicationDate`; quita el sort manual, usa `sort=publicationDate:desc`; fix de zona horaria en `formatSpanishDate`) | Fecha editorial (fuera de plan) |
| **`autoexplora-alfa`**: `app/blog/data/posts.ts` | Modificado (`Post.publishedAtISO` → `Post.dateISO`) | Fecha editorial (fuera de plan) |
| **`autoexplora-alfa`**: `app/blog/[slug]/page.tsx`, `app/sitemap.ts` | Modificado (usan `dateISO`/`publicationDate` en vez de metadatos de Strapi) | Fecha editorial (fuera de plan) |
| `src/lifecycles/defaultBannerOrder.ts` | Creado (asigna `order` al final vía `beforeCreate` cuando no viene explícito) | Banners (fuera de plan) |
| `src/api/banner/services/reorder.ts` | Modificado (extrae helper `renumberContiguous`; agrega `reorderBanners()`) | Banners (fuera de plan) |
| `src/admin-routes/registerBannerAdminRoutes.ts` | Creado (`GET /banners/list` vía Document Service, `POST /banners/reorder`, ambos admin-autenticados) | Banners (fuera de plan) |
| `src/bootstrap/ensureBannerViewConfiguration.ts` | Creado (quita `order` de `layouts.edit` del Content Manager) | Banners (fuera de plan) |
| `src/admin/extensions/BannersOrderPage.tsx` | Creado (página "Homepage banners": lista + drag & drop con `@dnd-kit`, usa `Layouts.Root/Header/Content`) | Banners (fuera de plan) |
| `package.json`, `package-lock.json` | Modificado (`@dnd-kit/core`, `@dnd-kit/sortable`, `@dnd-kit/utilities` como dependencias explícitas) | Banners (fuera de plan) |
| `src/index.ts` | Modificado (conecta `registerDefaultBannerOrder`, `registerBannerAdminRoutes`, `ensureBannerViewConfiguration`) | Banners (fuera de plan) |
| `src/admin/extensions/background.css` | **Eliminado** (contenido migrado a `injectBrandStyles.ts`) | Fix CSS producción (fuera de plan) |
| `src/admin/extensions/injectBrandStyles.ts` | Creado (inyecta el CSS de marca por JS en `bootstrap()`, en vez de por `import` de `.css`) | Fix CSS producción (fuera de plan) |

---

## Commits realizados

| Hash | Mensaje | Fecha |
|---|---|---|
| `4d30ad1` | Inicializar repositorio autoexplora-cms | 2026-07-16 |
| `b50a92e` | [cms-autoexplora] Fase 0 - Scaffold e infraestructura base | 2026-07-16 |
| `b44d14a` | [cms-autoexplora] Rehacer T-05 - Despliegue EC2 + Nginx + systemd (sin Docker) | 2026-07-17 |
| `c2131e2` (enginecx_prd) | cms-autoexplora Actualizar plan - Despliegue EC2 + Nginx + systemd | 2026-07-17 |
| `1b950ad` | [cms-autoexplora] Fix T-03 - Deshabilitar ACL en provider S3 | 2026-07-17 |
| `d209e3d` (enginecx_prd) | cms-autoexplora Actualizar avance y plan - T-03 verificado, bloqueo bucket policy | 2026-07-17 |
| `a9a759f` (enginecx_prd) | cms-autoexplora Confirmar modelo de publicación y rama base del sitio | 2026-07-17 |
| `18f22ad` | [cms-autoexplora] Fase 1 - T-06/T-07: rol Editor + content type Banner | 2026-07-17 |
| `281b6cb` | [cms-autoexplora] Fase 1 - T-08: validación de formato y peso de archivos | 2026-07-20 |
| `0fb79c5` (enginecx_prd) | cms-autoexplora Actualizar plan y avance - T-08 completada, hallazgo CORS S3 | 2026-07-20 |
| `8f352bb` | [cms-autoexplora] Fase 1 - T-09: reordenamiento automático de banners por vigencia | 2026-07-20 |
| `1e1a6a9` (enginecx_prd) | cms-autoexplora Actualizar plan y avance - T-09 completada | 2026-07-20 |
| `cd78590` | [cms-autoexplora] Fase 1 - T-10: publicación en dos etapas (contrato de API + webhook) | 2026-07-20 |
| `d1990e9` (enginecx_prd) | cms-autoexplora Actualizar plan y avance - T-10 completada, corrección de API v5 | 2026-07-20 |
| `414dddb` (enginecx_prd) | cms-autoexplora Registrar solicitud de aprovisionamiento EC2 a Alexis | 2026-07-20 |
| `c3337d6` (enginecx_prd) | cms-autoexplora Cerrar T-03 - CORS + CSP resueltos, miniaturas visibles | 2026-07-20 |
| `ea1c177` | [cms-autoexplora] Fix T-03 - Permitir buckets S3 en Content Security Policy | 2026-07-20 |
| `2fad324` (autoexplora-alfa) | [cms-autoexplora] Fase 1 - T-11: consumir banners del CMS en el home | 2026-07-20 |
| `ff1ad66` (enginecx_prd) | cms-autoexplora Actualizar plan y avance - T-11 completada | 2026-07-20 |
| `f2da662` | [cms-autoexplora] Fase 1 - T-12: registro de auditoría (completa P1) | 2026-07-20 |
| `e2daa1c` (enginecx_prd) | cms-autoexplora Actualizar plan y avance - T-12 completada, P1 completo | 2026-07-20 |
| `33ef5ea` | [cms-autoexplora] Mejora parcial de UI del admin (logo, color de marca, etiquetas en español) | 2026-07-24 |
| `d9c95a5` | [cms-autoexplora] Fase 2 - T-13: content type Article para el blog | 2026-07-24 |
| `1f569d3` (enginecx_prd) | cms-autoexplora Actualizar plan y avance - T-13 completada, inicia P2 | 2026-07-24 |
| `4a47d75` | [cms-autoexplora] Fase 2 - T-14: editor de bloques del cuerpo del artículo (embeds) | 2026-07-27 |
| `77688c6` (enginecx_prd) | cms-autoexplora Actualizar plan y avance - T-14 completada, pivote a Dynamic Zone | 2026-07-27 |
| `2cbd460` (autoexplora-alfa) | [cms-autoexplora] Fase 2 - T-15: consumir el blog del CMS en el sitio | 2026-07-28 |
| `f05fbe0` | [cms-autoexplora] Fase 2 - T-15: permisos del token de solo lectura del sitio | 2026-07-28 |
| `05f9c42` (enginecx_prd) | cms-autoexplora Actualizar plan y avance - T-15 completada, cierra Fase 2 | 2026-07-28 |
| `93aaf22` (enginecx_prd) | cms-autoexplora Completar tabla de commits realizados (T-13 a T-15) | 2026-07-28 |
| `0e6bc09` | [cms-autoexplora] Mejora de UI del admin: tema claro/oscuro, marca y textos | 2026-07-29 |
| `c439a83` (enginecx_prd) | cms-autoexplora Documentar segunda ronda de mejoras de UI del admin | 2026-07-29 |
| `6d9bd87` (autoexplora-alfa) | debug(temporal): logging en fetchHomeBanners para diagnosticar QA | 2026-07-31 |
| `757db46` (autoexplora-alfa) | Revert "debug(temporal): logging en fetchHomeBanners para diagnosticar QA" | 2026-07-31 |
| `3dbf45c` (enginecx_prd) | cms-autoexplora Documentar despliegue de QA (T-19 parcial, adelanta Fase 4) | 2026-07-31 |
| `468d8fa` (enginecx_prd) | Merge remote-tracking branch 'origin/main' | 2026-07-31 |
| `72d8bac` | [cms-autoexplora] Article: fecha de publicación editorial (Custom Field) | 2026-08-03 |
| `9565ade` (autoexplora-alfa) | [cms-autoexplora] Blog: usar la fecha editorial de Article (publicationDate) | 2026-08-03 |
| `ae6289e` | [cms-autoexplora] Banners: reorden por drag & drop y posición automática | 2026-08-03 |
| `208e530` | [cms-autoexplora] Admin: inyectar el CSS de marca por JS, no por import de .css | 2026-08-03 |

---

## Notas para quien retome el trabajo

- El repo `autoexplora-cms` vive en `~/Documents/BRICK-sites/autoexplora-cms` localmente (hermano de `autoexplora-alfa`).
- Rama activa: `feature/PJ5040-cms-autoexplora-mvp`.
- Postgres local: Postgres.app instalado en `/Applications/Postgres.app`; servidor se arranca manualmente con `pg_ctl -D ~/Library/Application\ Support/Postgres/var-16 -l logfile start` (no es un servicio automático del sistema).
- Base de datos local: `autoexplora_cms_dev`, usuario `strapi_cms` — credenciales en `.env` local (no versionado).
- ✅ **P1 completo (T-06 a T-12). P2: T-13 y T-14 completas.** Siguiente paso: T-15 (consumo del blog en el sitio).
- Auditoría (T-12): content type `AuditLog` (sin Draft & Publish), poblado vía `strapi.documents.use()` — `TRACKED_UIDS` en `src/lifecycles/auditLog.ts` ya incluye `api::article.article`; extender también al agregar StaticText (T-16), igual que con `EDITOR_MANAGED_CONTENT_TYPES`.
- **T-13 (Article):** schema en `src/api/article/content-types/article/schema.json` — `title`, `slug` (uid sobre `title`), `author`, `hero` (media, solo imagen, requerido, **independiente** del cuerpo), `content` (Dynamic Zone, ver T-14), `description`/`excerpt` (opcionales, para SEO/listado). El listado `/blog` ordena por más reciente primero y usa `hero` (no deriva de `content`) tanto para el hero banner del post como para la miniatura de la card; la URL del post se genera con `slug`.
- ⚠️ **T-14 (Article.content): NO es un campo `blocks` — es una Dynamic Zone.** `content` acepta `article.paragraph` (`text`), `article.heading` (`text`), `article.quote` (`text`, `author?`), `article.image` (`image`), `article.list` (`items`: repeatable `article.list-item` con `text`), `article.embed` (`url` — el proveedor NO se guarda, se detecta al renderizar).
- ✅ **T-15 completa:** el sitio replica la misma lógica de detección de proveedor por hostname en `lib/blog/embedProvider.ts` (YouTube → iframe `youtube.com/embed/{id}`; Instagram/Twitter-X/TikTok/Facebook → tarjeta con enlace, sin iframe garantizado). `BlogPostLayout.tsx` tiene un `case` por cada tipo de bloque del sitio (`paragraph/heading/blockquote/image/list/embed`); el mapeo desde `__component` de Strapi (ej. `"article.paragraph"`) vive en `lib/server/strapiApi.ts` (función `toContentBlock`), no en el componente de render. **Bugs de Strapi encontrados y corregidos (ver Decisiones):** `populate[hero]=*` falla para un campo media simple (usar `=true`); `publishedAt` es siempre `null` en `status=draft`, hay que reordenar en el cliente con `createdAt` como respaldo.
- 🔴 **Límite duro de Strapi 5.50.2 a tener en cuenta en cualquier trabajo futuro con campos `blocks`:** el editor de Bloques nativo permite registrar tipos de nodo personalizados en el ADMIN (`addRichTextBlocks`), pero el servidor los rechaza siempre al guardar (`blocks-validator.js`, enum cerrado sin extensión posible). Si en el futuro se necesita un tipo de bloque no nativo dentro de un campo rich text, la única ruta viable confirmada es una **Dynamic Zone de componentes**, no el campo `blocks`.
- ⚠️ **Contrato de API corregido:** usar `status=draft`/`status=published`, **no** `publicationState` (eso era Strapi v4; en v5 da error). `status=draft` muestra el borrador de TODO, incluso lo ya publicado — es lo que usa el preview del sitio.
- ✅ Modelo de publicación confirmado (2026-07-17): Draft & Publish + webhook (PLAN.md §3).
- ✅ **Ambiente de QA desplegado y accesible (2026-07-31):** `https://qa-autoexplora-cms.govirtual.com.mx:8080/admin` (nota el puerto `:8080`, no es el 443 estándar — decisión de Alexis). Servidor: EC2 Ubuntu 26.04, código en `/usr/local/qa-autoexplora-cms`, rama `feature/PJ5040-cms-autoexplora-mvp`, proceso `systemd` (`strapi.service`), Nginx como reverse proxy, certificado SSL real (Certbot, renovación automática). Base de datos `qa_autoexplora_cms`/usuario `strapi_cms` con backup diario local (`/usr/local/bin/backup-qa-autoexplora-cms.sh`, cron 3:00 AM, retención 14 días — **no hay backup externo/S3 todavía**, pendiente de un bucket privado). Acceso SSH del equipo vía Termius; despliegue de código vía Deploy Key de solo lectura en GitHub. El preview de Vercel de `autoexplora-alfa` (rama `feature/PJ5040-cms-autoexplora-mvp`) ya consume este CMS QA real vía variables de entorno con scope "Preview" (`STRAPI_API_URL`, `STRAPI_API_TOKEN`, `STRAPI_PUBLICATION_STATUS=published`) — **nunca aplicar estas variables a "Production"** en Vercel, apuntaría el sitio real a un CMS de pruebas.
- ⚠️ **Pendiente de T-19:** instancia de producción (EC2 separada, ya confirmada con Alexis) — repetir todo el proceso de despliegue en una instancia nueva cuando se decida ir a prod. Alertas de facturación de AWS también pendientes (solicitadas a Alexis, sin respuesta al cierre de esta nota).
- ✅ Rama base del sitio confirmada (2026-07-17): `dev`. Desde T-11, `autoexplora-alfa` también vive en `feature/PJ5040-cms-autoexplora-mvp` (creada desde `dev`), en `~/Documents/BRICK-sites/autoexplora-alfa`.
- ⚠️ **`autoexplora-alfa` ya está en producción** (rama `post-lanzamiento`, con commits ajenos al CMS). Antes de T-15 se confirmó que `dev` ya estaba sincronizada con `post-lanzamiento` y se mergeó `dev` hacia `feature/PJ5040-cms-autoexplora-mvp` (sin conflictos). Cualquier trabajo futuro sobre esta rama debería repetir ese chequeo si ha pasado tiempo — comparar `git log origin/dev..origin/post-lanzamiento` antes de asumir que siguen sincronizadas.
- Despliegue del CMS: **EC2 + Nginx + systemd, sin Docker**. La instancia no existe aún — solicitada a Alexis Herrera (2026-07-20). `deploy/nginx.conf` y `deploy/strapi.service` listos pero no verificados en una instancia real.
- ✅ Bucket S3: subida, lectura pública, CORS y CSP — todo verificado (2026-07-17 a 2026-07-20). Sin bloqueos de S3 activos.
- Banner (T-07) usa **Dynamic Zone** (`content`, componentes `banner.image-content`/`banner.video-content`), no campos planos — cualquier trabajo futuro sobre Banner (T-13 Article puede reusar el patrón) debe considerar esta estructura, no la original del PRD/plan.
- Validación de archivos (T-08) es un hook **global** (`src/lifecycles/validateUploadedFiles.ts`), no específico de Banner — ya cubre cualquier content type futuro que suba imágenes/video.
- El rol Editor recibe permisos por código en `src/bootstrap/ensureEditorPermissions.ts` — al agregar Article (T-13) o StaticText (T-16), extender `EDITOR_MANAGED_CONTENT_TYPES` ahí, no dar permisos manualmente desde el admin.
- T-09 (reordenamiento) corre vía cron cada 5 min (`config/cron-tasks.ts`) + hook reactivo (`src/lifecycles/recompactBannersOnChange.ts`, con guardia anti-reentrancia). Banner expirado se despublica (no se elimina), preservando el borrador.
- Primer usuario admin de Strapi ya creado por el programador directamente en `http://localhost:1337/admin`.
- **T-11 pendiente de conectar a futuro:** el webhook `site-revalidation` (T-10) sigue sin registrarse porque `SITE_REVALIDATE_WEBHOOK_URL` está vacía — se decidió deliberadamente no construir el endpoint de revalidación instantánea en T-11 (ISR de 2 min es suficiente por ahora); queda como mejora aditiva para T-18 (endurecimiento), sin rework.
- El sitio (`autoexplora-alfa`) consume Strapi vía `lib/server/strapiApi.ts` — banners desde `app/page.tsx`, blog desde `app/blog/page.tsx`/`app/blog/[slug]/page.tsx`/`app/sitemap.ts` — todo server-side, sin ruta `/api/*` intermedia; el token nunca llega al navegador.
- ✅ Token de solo lectura para el sitio (`autoexplora-alfa-site-read`) ya tiene permisos de Banner **y Article** (`find`/`findOne` de ambos) — automatizado por `src/bootstrap/ensureSiteApiTokenPermissions.ts` en el CMS, idempotente. Al agregar StaticText (T-17), extender `SITE_READ_ACTIONS` ahí, no dar el permiso a mano desde el admin.
- ✅ **Article ya no deriva su fecha de metadatos de Strapi:** el campo editorial es `publicationDate` (Custom Field `global::todayDate`, se autorrellena con "hoy" al crear, 100% editable incluso a fecha pasada). Define tanto la fecha mostrada en el sitio como el orden de `/blog` (`sort=publicationDate:desc`). Si se crea contenido de prueba directo en BD (sin pasar por el formulario), recordar poblar `publicationDate` — un valor nulo aparece primero en el listado (Postgres ordena `NULL` primero en `DESC`).
- ✅ **`order` de Banner ya no se edita a mano:** se asigna solo al final al crear (`src/lifecycles/defaultBannerOrder.ts`) y se quitó de `layouts.edit` del Content Manager (`src/bootstrap/ensureBannerViewConfiguration.ts`). La única forma de cambiar la posición de un banner existente es la página "Homepage banners" del admin (drag & drop, `src/admin/extensions/BannersOrderPage.tsx`), que llama a `POST /banners/reorder`. Cualquier vista custom futura del admin que necesite leer un dynamiczone con sus medios debe usar el Document Service directo (ver `src/admin-routes/registerBannerAdminRoutes.ts`), no el `find` del Content Manager — este último limita el populate a un nivel y nunca trae los medios dentro del componente.
- 🔴 **CSS que dependa de `app.tsx`/`bootstrap()` para verse siempre debe inyectarse por JS, no por `import` de `.css`:** un `import './archivo.css'` ahí nunca se carga en un build de producción (`strapi build`/`strapi start`) — solo funciona en `strapi develop`. Ver `src/admin/extensions/injectBrandStyles.ts` como el patrón a seguir (y su comentario, con el diagnóstico completo). Si se agrega más CSS de marca en el futuro, va ahí, no en un archivo `.css` nuevo importado desde `app.tsx`.
- ⚠️ **Permisos de `/usr/local/qa-autoexplora-cms` (y de cualquier instancia futura, incluyendo producción):** el directorio queda `ubuntu:strapi` (dueño `ubuntu` para poder hacer `git pull`/`npm ci`/`npm run build` con su propio acceso a GitHub; grupo `strapi` para que el servicio siga leyendo todo igual). Si se re-clona desde cero, el clon inicial se hace como `ubuntu` y **no** hay que dejar todo el árbol como `strapi` de dueño — repetir el `chown -R ubuntu:strapi` después de crear el usuario de sistema, antes de que alguien intente actualizar por primera vez.

---

*Actualizado automáticamente por Claude Code — Engine CX*
